{
	"$schema": "https://turbo.build/schema.json",
	"ui": "tui",
	"tasks": {
		"dev": {
			"dependsOn": ["^dev"],
			"cache": false,
			"persistent": true,
			"inputs": ["$TURBO_DEFAULT$", ".env"],
			"env": ["DATABASE_URL", "DATASET_URL", "JWT_SECRET", "JWT_EXPIRES_IN"]
		},
		"lint": { "dependsOn": ["^lint"] },
		"format": { "dependsOn": ["^format"] }
	}
}
